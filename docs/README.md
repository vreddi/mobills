# Documentation

Guides for working with the **mobills** monorepo.

## CLI

- [Running the CLI locally](./cli/local-install.md) — build the `mobills` CLI
  from source and put it on your `PATH`, refresh it after changes, unlink it,
  and switch to the published package.
